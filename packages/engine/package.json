{
  "name": "@streamline/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic financial core: money classes, statistics, control charts, DiD, gates, the mint, overlap, state machines, detectors, ledger.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.0.0",
    "@types/node": "^22.19.0"
  }
}
