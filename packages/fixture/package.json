{
  "name": "@streamline/fixture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rosewood Group: a deterministic, clearly synthetic three-location restaurant register and its intervention history.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@streamline/engine": "workspace:*",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.0.0",
    "@types/node": "^22.19.0"
  }
}
