# Copy to .env (optional). Defaults work for the demo.
# SQLite file for the demo database. Relative to packages/db unless absolute.
STREAMLINE_DB_PATH=./data/streamline.db
# Secret used to sign the demo session cookie. Change for any shared deployment.
STREAMLINE_SESSION_SECRET=dev-only-change-me
NEXT_TELEMETRY_DISABLED=1
